1. 도시의 개수를 입력
2. 비용테이블 입력
3. 도시의 개수를 표현하는 비트 마스크 생성
4. 함수 호출

함수(인자: 방문 마을, 지나온 마을) ->

# 처음에 방문하는 곳이 0이기 때문에 0 과 000001을 입력

1. 방문한 마을이 total일 때 비용테이블[방문위치][0]이 0 이상이면 비용테이블[방문위치][0] 반환
(0으로 되돌아가는 비용)

아니면 20202020 반환(의미없는 큰 수로 추정)

2.  메모[현재위치][방문마을] 의 값이 0 이상이면 메모[인덱스][방문마을] 반환
이거 무슨 의민지 모르겠음

3. tmp는 20202020 의미없는 큰 수로 추정

4. 1 에서 n-1 까지 반복, i,

i를 방문한 적이 있거나, i로 갈 수 가 없으면 생략

tmp에 최저 비용 저장

반복 끝

5. 메모[현재][방문마을]에 최저 값 저장(이건 따로 반환 안해도 어차피 지 맘대로 저장됨)

6. 최저 값 반환